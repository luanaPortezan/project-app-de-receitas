import Header from '../components/Header';

function Profile() {
  return (
    <main>
      <Header pages isSearch="profile">
        <h1>Profile</h1>
      </Header>
    </main>
  );
}

export default Profile;
